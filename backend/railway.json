{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python -m pip install --upgrade pip && python -m pip install -e . && python -m pip install alembic uvicorn"
  },
  "deploy": {
    "startCommand": "python -m pip install --upgrade pip && python -m pip install -e . && python scripts/seed_demo_users.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}